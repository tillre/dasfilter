- hosts: db
  remote_user: deploy
  vars:
    couchdb_version: 1.6.1
    proxy_sites:
      - db-site
    firewall_open_ports:
      - 80

  roles:
    - nginx
    - proxy
    - couchdb
    - firewall
